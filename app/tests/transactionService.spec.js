describe('transactionService factory', function() {
    var authenticator;
    var urlProvider;

    // Before each test load our api.users module
    beforeEach(angular.mock.module('okoa.home.services'));

    // Before each test set our injected Users factory (_Users_) to our local Users variable
    beforeEach(inject(function(_authenticator_) {
        authenticator = _authenticator_;
    }));

    // Wrap the parameter in underscores
    //beforeEach( inject( function(_urlProvider_){
    //    urlProvider = _urlProvider_;
    //}));

    //inject dependecies
    it('should ....', inject(function($http,urlProvider) {
        $http = _$http_;
        urlProvider = _urlProvider_;
    }));

    // A simple test to verify the Users factory exists
    it('should exist', function() {
        expect(authenticator).toBeDefined();
    });
    // Before each test load our api.users module
    beforeEach(angular.mock.module('okoa.home.services'));

    // Before each test set our injected Users factory (_Users_) to our local Users variable
    beforeEach(inject(function(_authenticator_) {
        authenticator = _authenticator_;
    }));

    // Wrap the parameter in underscores
    //beforeEach( inject( function(_urlProvider_){
    //    urlProvider = _urlProvider_;
    //}));

    //inject dependecies
    it('should ....', inject(function($http,urlProvider) {
        $http = _$http_;
        urlProvider = _urlProvider_;
    }));

    // A simple test to verify the Users factory exists
    it('should exist', function() {
        expect(authenticator).toBeDefined();
    });
    // Before each test load our api.users module
    beforeEach(angular.mock.module('okoa.home.services'));

    // Before each test set our injected Users factory (_Users_) to our local Users variable
    beforeEach(inject(function(_authenticator_) {
        authenticator = _authenticator_;
    }));

    // Wrap the parameter in underscores
    //beforeEach( inject( function(_urlProvider_){
    //    urlProvider = _urlProvider_;
    //}));

    //inject dependecies
    it('should ....', inject(function($http,urlProvider) {
        $http = _$http_;
        urlProvider = _urlProvider_;
    }));

    // A simple test to verify the Users factory exists
    it('should exist', function() {
        expect(authenticator).toBeDefined();
    });
    // Before each test load our api.users module
    beforeEach(angular.mock.module('okoa.home.services'));

    // Before each test set our injected Users factory (_Users_) to our local Users variable
    beforeEach(inject(function(_authenticator_) {
        authenticator = _authenticator_;
    }));

    // Wrap the parameter in underscores
    //beforeEach( inject( function(_urlProvider_){
    //    urlProvider = _urlProvider_;
    //}));

    //inject dependecies
    it('should ....', inject(function($http,urlProvider) {
        $http = _$http_;
        urlProvider = _urlProvider_;
    }));

    // A simple test to verify the Users factory exists
    it('should exist', function() {
        expect(authenticator).toBeDefined();
    });
});
